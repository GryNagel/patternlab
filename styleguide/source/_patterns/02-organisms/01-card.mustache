<div class="c-card c-card--equal-height">
    <img class="o-image" alt="{{img.square.src}}" src="{{img.square.src}}"/>
    <header class="c-card__header">
        <h2 class="c-heading">
            {{ title }}
            <div class="c-heading__sub"> {{ title }}</div>
        </h2>
    </header>
    <div class="c-card__body">
        {{>atoms-text}}
        <a  class="" href="#">Read more</a>
    </div>
</div>